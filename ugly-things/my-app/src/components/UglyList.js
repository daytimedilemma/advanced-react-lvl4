import React from "react";

export default function UglyList(props) {
    return (
            <ul>
                {/* <h3>{props.uglyThing.title}</h3>
                <img src={`${props.uglyThing.imgUrl}`} />
                <p>{props.}</p> */}
            </ul>
        )
}